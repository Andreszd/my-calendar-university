const typesHeading = {
    h1: "h1",
    h2: "h2",
    h3: "h3",
};

const sizes = {
    sm: 14,
    md: 15,
    lg: 17,
};

const colors = ["base", "primary", "secondary"];

export { sizes, typesHeading, colors };
